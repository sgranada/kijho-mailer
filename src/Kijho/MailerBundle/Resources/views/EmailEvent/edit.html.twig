{% extends 'KijhoMailerBundle:Default:main.html.twig' %}
{% block title %}{{'kijho_mailer.email_event.edit'|trans}}{% endblock %}
{% block title_iframe %}{{'kijho_mailer.email_event.edit'|trans}}{% endblock%}
{% block header_content %}
    <h2 class="pull-left">{{'kijho_mailer.email_event.edit'|trans}}</h2>
    <div class="btn-group pull-right top-margin" role="group" aria-label="...">
        <a href="{{ app.request.headers.get('referer') }}" class="btn btn-default">{{'kijho_mailer.global.back'|trans}}</a>
    </div>
{% endblock %}
{% block main_content %}
    <ol class="col-lg-12 breadcrumb">
        <li><a href="{{path('kijho_mailer_homepage')}}">{{'kijho_mailer.global.home'|trans}}</a></li>
        <li><a href="{{path('kijho_mailer_email_event')}}">{{'kijho_mailer.email_event.email_events'|trans}}</a></li>
        <li class="active">{{'kijho_mailer.email_event.edit'|trans}}</li>
    </ol>
        
        <div class="col-lg-12">
            <form role="form" action="{{path('kijho_mailer_email_event_update',{'emailEventId':emailEvent.id})}}" class="form-horizontal" method="post" {{ form_enctype(form) }}>
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_label(form.name) }}
                        {{ form_widget(form.name) }}
                        {{ form_errors(form.name) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.template) }}
                        {{ form_widget(form.template) }}
                        {{ form_errors(form.template) }}
                    </div>
                    <div class="form-group">
                        {{ form_widget(form._token) }} 
                        <button type="submit" class="btn btn-primary pull-right">{{'kijho_mailer.global.update'|trans}}</button>
                    </div>
                </div>
            </form>
        </div>
{% endblock %}

{% block scripts %}
{% endblock %}